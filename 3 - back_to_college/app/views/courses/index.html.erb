<h1> ALL OF THE COURSES </h1>

<% @courses.each do |course| %>
  <%= course.name %> <%= link_to "More Info", course_path(course.id) %><br>
<% end %><br>

<%= link_to "Add New Course", new_course_path%><br>
<%= link_to "View Students", students_path %>
